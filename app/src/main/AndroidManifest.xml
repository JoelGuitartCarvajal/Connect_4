<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.connect_4">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.Dark.NoActionBar">
        <activity
            android:name=".DetallActivity"
            android:exported="false" />
        <activity
            android:name=".ConsultActv"
            android:exported="false" />
        <activity android:name=".PreferencesActivity" />
        <activity android:name=".JocActv" />
        <activity android:name=".ResultatPartida" />
        <activity android:name=".Configuracio" />
        <activity android:name=".Ajuda" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>